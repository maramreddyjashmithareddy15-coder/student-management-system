{
  "name": "college-student-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack College Student Management System",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && node src/server.js"
  }
}
