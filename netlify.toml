# Netlify Configuration for College Student Management System

[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Single Page Application (SPA) Routing for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
